# Devlog: v26.1-26.0 Command Palette

Date: 2026-08-24
Branch: `feat/v26.1-26.0-command-palette`
Workflow: Planning > Research > Development > Testing > Tuning > Release

## Pre-implementation Audit (BC Development Process compliance)

<!-- GitHub@NDBlockConnect | BlockConnect@StarsailsClover -->

1. **Existing code audit**: `grep CommandPalette|palette|Ctrl+K` across
   `src/**` returned ZERO hits. No partial implementation exists; the
   feature starts from a clean slate. (Prior orphaned-code incidents --
   GizmoInteraction in v10, view-mode controls lost in v20.1 -- informed
   the decision to audit before building.)
2. **Git control audit**:
   - SSH signing: `commit.gpgsign=true`, `gpg.format=ssh` -- compliant.
   - Branch policy: previous increments committed directly to `main`
     (violation of BC Git Control rule 1). Corrected for this increment:
     all work on `feat/v26.1-26.0-command-palette`, merged to `main`
     at release with a merge commit.
   - Commit language: English -- compliant.
3. **Escape-chain audit** (integration point): current order is
   `close menu > close cheat sheet > cancel gizmo drag > deselect`.
   Palette close slots in after cheat sheet.
4. **Shortcut registry audit**: `SHORTCUTS` registry exists and is
   suitable as the palette's shortcut-badge source.
5. **Conflict check**: no existing `Ctrl+K` binding; no conflicts.

## Scope (per UX roadmap v26.1-26.0)

- Ctrl+K fuzzy command palette: primitives, view presets, projection
  toggle, save/load/new/export, gizmo modes, grid/play toggles,
  workspaces, cheat sheet, plugin tools; shortcut badges; Enter runs
  top hit; arrows navigate; Recent (last 5) persisted.
- Hierarchy live search filter.

## Test plan

`tests/e2e/v29-palette.spec.ts` (planned, 8 cases).
